services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 5432
      DB_NAME: yangyang
      DB_USER: postgres
      DB_PASSWORD: ""
    extra_hosts:
      - "host.docker.internal:host-gateway"
